[build]
  base = "Frontend"
  command = "npm run build"
  publish = "Frontend/dist"

[build.environment]
  NODE_VERSION = "20"

# Redirect rules for React Router Single/Multi-Page Routing
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
